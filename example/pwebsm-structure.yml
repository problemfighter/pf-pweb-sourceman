
before_start:
  - npm install -g yarn

app_dependencies:
  - dir: "application"
    setup-py: "develop"
    branch: "dev"
    run-py-script:
      - setup.py develop
    mode:
      - dev

pip_install:
  - mode:
      - all
    packages:
      - flask
  - mode:
      - prod
    packages:
      - PF-PY-File

dependencies:
  - dir: "dev-dependencies"
    setup-py: "develop"
    git-clone:
      - branch: "dev"
        mode:
          - dev
        repo:
          - name: "xyz"
            url: ""
            branch: ""
            script:
              - x
              - y

before_end:
  - yarn install